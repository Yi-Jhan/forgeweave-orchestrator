{
  "name": "forgeweave-orchestrator",
  "version": "0.0.0",
  "private": true,
  "description": "CLI-first runtime-agnostic multi-agent orchestration toolkit.",
  "license": "UNLICENSED",
  "packageManager": "pnpm@7.27.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=22"
  }
}
