{
  "name": "@forgeweave/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "forgeweave": "./dist/main.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "dependencies": {
    "@forgeweave/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --root ../.. apps/cli/src/cli.test.ts",
    "smoke": "node dist/main.js --help && node dist/main.js --version && node dist/main.js init --project-root ../../examples/acc --dry-run && node dist/main.js init --project-root ../../examples/minimal-project --dry-run"
  }
}
