# ACC Fixture

這裡是 sanitized ACC fixture，不是真實 ACC source tree。

用途：

- Phase 1 onboarding fixture。
- Phase 2 review fixture。
- Phase 4 single-page migration fixture。

規則：

- 不要複製真實 ACC source files。
- 不要 commit proprietary code。
- 不要 commit 真實 route、API、token、private URL 或本機路徑。
- 只放合成範例、placeholder structure、測試用 manifest。
- 真實 ACC 驗證必須透過 `FORGEWEAVE_ACC_ROOT`。
